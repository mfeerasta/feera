# /srv/soketi/.env on 46.225.157.75
# Generate fresh secrets per environment.
#   SOKETI_APP_ID=$(openssl rand -hex 16)
#   SOKETI_KEY=$(openssl rand -hex 24)
#   SOKETI_SECRET=$(openssl rand -hex 32)
SOKETI_APP_ID=
SOKETI_KEY=
SOKETI_SECRET=
